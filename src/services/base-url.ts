const baseUrl = 'http://localhost:3000';

export const URLS = {
  // =========================== Auth ===================== //
  LOGIN: `${baseUrl}/auth/login`,
  REGISTER: `${baseUrl}/auth/register`,
};
